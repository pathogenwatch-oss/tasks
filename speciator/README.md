# Specieator

## Input

a stream of fasta file from stdin

## Output

JSON with the following keys:
```
{
  organismName,
  organismId,
  speciesId,
  speciesName,
  genusId,
  genusName,
  referenceId,
  mashDistance,
  pValue,
  matchingHashes,
}
```
